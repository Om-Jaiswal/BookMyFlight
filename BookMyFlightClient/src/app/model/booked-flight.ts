export interface BookedFlight {
    flightNumber: string,
    airline: string,
    departureTime: string,
    arrivalTime: string,
    date: string,
    classAndPrice: any,
    passengerCount: number
}